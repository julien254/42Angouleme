# **************************************************************************** #
#                                                                              #
#                                                         :::      ::::::::    #
#    libft_lexique                                      :+:      :+:    :+:    #
#                                                     +:+ +:+         +:+      #
#    By: judetre <julien.detre.dev@gmail.com>       +#+  +:+       +#+         #
#                                                 +#+#+#+#+#+   +#+            #
#    Created: 2023/11/01 12:29:57 by judetre           #+#    #+#              #
#    Updated: 2023/11/01 18:50:43 by judetre          ###   ########.fr        #
#                                                                              #
# **************************************************************************** #

description fonction de la libft :

ft_atoi.				Recupere la premiere occurance d'un nombre dans une 
						chaine de caractere et le transforme en int.

ft_bzero.				Set la memoire a 0 sur n octet.

ft_calloc.				Alloue de la memoire et set tous les octets a 0.

ft_isalnum.				Return 1 si le caractere est numerique ou/et de lettres.

ft_isalpha.				Return 1 si la caractere est une lettres.

ft_isascii.				Return 1 si le caracteres est
						de la table ascii de 0 a 127 (non etendu).

ft_isdigit.				Return 1 si le caractere est un chiffre.

ft_isprint.				Return 1 si le caractere est printable.

ft_strlen.				Return la size d'une str.

ft_memset.				Set le nombre d'octet voulut a une valeur definis.

ft_memcpy.				Copie la memoire sur le pointeur envoyer dans la fonction
						sur n octet.

ft_memmove.				Same of memcpy, mais gere le chevauchement de la dest et 
						de la source.

ft_strlcpy.				Copie une chaine de caractere dans la destination donner.

ft_strlcat.				Concatene deux chaine de charactere (dest + src).

ft_toupper.				Change une minuscule en majuscule.

ft_tolower.				Change une majuscule en minuscule.

ft_strchr.				Trouve la premiere occurance du caractere donne dans la chaine.

ft_strrchr.				Trouve la derniere occurance du caractere donne dans la chaine.

ft_strncmp.				Compare deux chaine de caracteres et donne leur difference ascii.

ft_memchr.				Same of strchr, mais gere tous type d'adresse.

ft_memcmp.				Same of strcmp, mais gere tous type d'adresse.

ft_strnstr.				Cherche un pattern dans une str sur n octet.

ft_strdup.				Copies une str dans un malloc.

ft_substr.				Alloue (avec malloc(3)) et retourne une chaîne de
					caractères issue de la chaîne ’s’.
					Cette nouvelle chaîne commence à l’index ’start’ et
					a pour taille maximale ’len’.
					
ft_strjoin.				Alloue (avec malloc(3)) et retourne une nouvelle
					chaîne, résultat de la concaténation de s1 et s2.
					
ft_strtrim.				Alloue (avec malloc(3)) et retourne une copie de
					la chaîne ’s1’, sans les caractères spécifiés
					dans ’set’ au début et à la fin de la chaîne de
					caractères.
					
ft_split.				Alloue (avec malloc(3)) et retourne un tableau
					de chaînes de caractères obtenu en séparant ’s’ à
					l’aide du caractère ’c’, utilisé comme délimiteur.
					Le tableau doit être terminé par NULL.

ft_itoa.				Alloue (avec malloc(3)) et retourne une chaîne
					de caractères représentant l’entier ’n’ reçu en
					argument. Les nombres négatifs doivent être gérés.
					
ft_strmapi.				Applique la fonction ’f’ à chaque caractère de la
					chaîne de caractères passée en argument pour créer
					une nouvelle chaîne de caractères (avec malloc(3))
					résultant des applications successives de ’f’.
					
ft_striteri.				Applique la fonction ’f’ à chaque caractère de la
					chaîne de caractères transmise comme argument,
					et en passant son index comme premier argument.
					Chaque caractère est transmis par adresse à ’f’
					afin d’être modifié si nécessaire.
					
ft_putchar_fd.				Écrit le caractère ’c’ sur le descripteur de
					fichier donné.
					
ft_putstr_fd.				Écrit la chaîne de caractères ’s’ sur le
					descripteur de fichier donné.
					
ft_putendl_fd.				Écrit La chaîne de caractères ’s’ sur le
					descripteur de fichier donné suivie d’un retour à
					la ligne.
					
ft_putnbr_fd.				Écrit l’entier ’n’ sur le descripteur de fichier
					donné.
					
ft_lstnew.				Alloue (avec malloc(3)) et renvoie un nouvel
					élément. La variable membre ’content’ est
					initialisée à l’aide de la valeur du paramètre
					’content’. La variable ’next’ est initialisée à
					NULL.
					
ft_lstadd_front.			Ajoute l’élément ’new’ au début de la liste.

ft_lstsize.				Compte le nombre d’éléments de la liste.

ft_lstadd_back.				Ajoute l’élément ’new’ à la fin de la liste.

ft_lstdelone.				Libère la mémoire de l’élément passé en argument en
					utilisant la fonction ’del’ puis avec free(3). La
					smémoire de ’next’ ne doit pas être free.

ft_lstclear.				Supprime et libère la mémoire de l’élément passé en
					paramètre, et de tous les éléments qui suivent, à
					l’aide de ’del’ et de free(3)
					Enfin, le pointeur initial doit être mis à NULL.
									
ft_lstlast.				Renvoie le dernier élément de la liste.

ft_lstiter.				Itère sur la liste ’lst’ et applique la fonction
					’f’ au contenu chaque élément.
					
ft_lstmap.				Itère sur la liste ’lst’ et applique la fonction
					’f ’au contenu de chaque élément. Crée une nouvelle
					liste résultant des applications successives de
					’f’. La fonction ’del’ est là pour détruire le
					contenu d’un élément si nécessaire.
